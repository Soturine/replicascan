# ReplicaScan v0.4.0 — implementation matrix

Repository implementation is separated from external validation.

| Area | State | Evidence |
|---|---|---|
| Product/Android identity | Implemented | resources, packages, manifest, version catalog |
| ML Kit Document Scanner as primary scan | Implemented | `feature-home/DocumentScanner.kt`; cancel/empty are no-ops |
| Heuristic detector, CameraX, scan modes | Removed | detector, profile router, `feature-camera`, `ScanMode`, CAMERA permission |
| Fallback when the scanner is unavailable | Implemented | system camera (`TakePicture`) or photo picker, then manual crop |
| Manual crop | Implemented | full-page default, RTL-correct handles, accessibility actions |
| OCR routing | Implemented | one recognizer via `OcrScriptPolicy`; readiness via `ModuleInstallClient` |
| Lossless export path | Implemented | in-memory render, JPEG byte copy, fail-fast cleanup; instrumented tests |
| Localization (12 locales) | Implemented, not natively reviewed | rewritten catalogs; localization gate |
| Deprecated APIs/warnings | Implemented | forced recompile of debug+release: 0 Kotlin warnings |
| Release identity from manifest | Implemented | `tools/release_artifact.py`, consistency gate, tests |
| API 36 instrumentation | Implemented in CI, not executed locally | managed device + KVM; local host lacked disk space |
| API 35 compatibility | Scheduled | managed device workflow |
| GitHub evaluation release | Owner-published | ADR 0003, verified by `verify-published` on tag push |
| Camera/document/OCR physical corpus | Pending owner validation | real devices required |
| TalkBack, 200% font, RTL physical QA | Pending owner validation | cannot be replaced by CI |
| Play signing/AAB/R8/Data safety | Out of scope | Play hardening milestone |
